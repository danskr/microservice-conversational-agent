apiVersion: v1
kind: Service
metadata:
  name: conversational-agent
  namespace: conversational-layer
spec:
  type: NodePort
  selector:
    app: conversational-agent
  ports:
    - name: http
      port: 2024
      targetPort: 2024
      nodePort: 30204
