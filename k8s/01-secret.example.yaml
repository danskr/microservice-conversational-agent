# Example only. Do NOT commit real keys.
# Prefer: export OPENAI_API_KEY=... LANGSMITH_API_KEY=... && ./scripts/create-secrets.sh
apiVersion: v1
kind: Secret
metadata:
  name: conversational-agent-secrets
  namespace: conversational-layer
type: Opaque
stringData:
  OPENAI_API_KEY: "replace-me"
  LANGSMITH_API_KEY: "replace-me"
